import { CoreDirDirective } from './core-dir.directive';

describe('CoreDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CoreDirDirective();
    expect(directive).toBeTruthy();
  });
});
